fn main() {
    let mut i: usize = 0;
    println!("{}", i as isize - 1);
    println!("{}", ((i as isize - 1) % 3 as isize));
    // i = ((i as isize - 1) % 3 as isize) as usize;
    // println!("{}", i);
    // i = ((i as isize - 1) % 3 as isize) as usize;
    // println!("{}", i);
    // i = ((i as isize - 1) % 3 as isize) as usize;
    // println!("{}", i);
    // i = ((i as isize - 1) % 3 as isize) as usize;
    // println!("{}", i);
}